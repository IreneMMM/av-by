# Локально: скопируйте этот файл в .env и подставьте реальные значения.
# Jenkins: .env не нужен — задайте те же переменные через Credentials (см. README.md).

VALID_LOGIN=your-email@example.com
VALID_PASSWORD=your-password
